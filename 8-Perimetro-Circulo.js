const pi = 3.14;
let diamentroCirculo = 10;
let raioCirculo = diamentroCirculo / 2;

perimetro = pi * diamentroCirculo;
area = pi * Math.pow(raioCirculo, 2);

console.log(`O perímetro e a área de um círculo de ${diamentroCirculo} cm de diametro é respectivamente, ${perimetro} cm e ${area} cm`);